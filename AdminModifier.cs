namespace SAE_IHM
{
    partial class AdminModifier
    {
        // Add the missing method definition for AdminModifier_FormClosing
        private void AdminModifier_FormClosing(object sender, FormClosingEventArgs e)
        {
            // Add any necessary logic here, or leave it empty if no specific action is required
        }
    }
}
